/* input:
starting point x,y
direction: N,W,S,E
commands: string[]

output:
f, b - forward backward
l, r - left, right
*/

describe("mars rover", () => {
  it("should move forward by one tile", () => {
    const x = 1;
    const y = 1;
    const direction = "N";
    const commands = ["f"];

    const expectedX = 1;
    const expectedY = 2;


    expect(true).toBe(false);
  });
});
